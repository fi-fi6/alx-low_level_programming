#include "main.h"

/**
  * add - adds two integers and returns the result
  * @first: first integer
  * @sec: second integer
  * Return: the result
  */

int add(int first, int sec)
{
	return (first + sec);
}
